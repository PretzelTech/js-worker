const jobArgs = process.argv.slice(2);
console.log(jobArgs);

//TODO: do a dynamic require based on the name of the script we want to run
//and run call on it in order to pass down the array of the arguments from the
//job
